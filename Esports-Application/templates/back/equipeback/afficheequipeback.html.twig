{% extends 'base.html.twig' %}


{% block stylesheets %}

    <link href="{{ asset('vendors/feather/feather.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/ti-icons/css/themify-icons.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/css/vendor.bundle.base.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/datatables.net-bs4/dataTables.bootstrap4.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/ti-icons/css/themify-icons.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('js/select.dataTables.min.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('css/vertical-layout-light/style.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('images/favicon.png') }}" type="text/css" rel="shortcut icon" />





{% endblock %}

{% block body %}






    <body>



    <div class="col-lg-12 stretch-card">
        <div class="card">
            <div class="card-body" >
                <h4 class="card-title">Les Equipes E-Tsports</h4>
                <a class="btn btn-outline-dark " href="{{ path('ajouterequipeback') }}" ><i class="fa-solid fa-circle-plus"></i> Ajouter un utilisateur</a>

                <p class="card-description">
                    </code>
                </p>
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                        <tr>

                            <th>ID</th>
                            <th>Nom Equipe</th>

                            <th>Email de L'equipe</th>
                            <th>Jeu de L'equipe</th>
                            <th>Users</th>
                            <th>delete</th>
                            <th>update</th>
                            <th>ajouter des joueurs </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for u in u %}
                            <tr >
                                <td>
                                    {{ u.id }}
                                </td>

                                <td>
                                    {{ u.nomEquipe }}
                                </td>

                                <td>
                                    {{ u.email}}
                                </td>

                                <td>
                                    {{ u.jeu}}
                                </td>
                                <td>

                                    {% for user in u.users %}

                                        {% if user is not null %}

                                            <br>
                                                {{ user.nom }}
                                            </br>
                                        {% endif %}
                                    {% endfor %}

                                </td>



                                <td>
                                    <a href="{{ path('deletequipeback',{'id':u.id}) }}"> Delete</a>
                                </td>

                                <td>
                                    <a href="{{ path('modifierequipeback',{'id':u.id}) }}"> Modify</a>
                                </td>
                                <td>
                                    <a href="{{ path('noteamback',{'id':u.id}) }}"> ajouter joueur a l'equipe</a>
                                </td>

                            </tr>
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    </div>
    </div>









    </table>
    <a href="{{ path('aback') }}"> ajouter membre</a>
    <a href="{{ path('ajouterequipeback') }}"> ajouter equipe</a>

    </body>
{% endblock %}





