







<nav class="nk-navbar nk-navbar-top nk-navbar-sticky nk-navbar-autohide">
    <div class="container">
        <div class="nk-nav-table">

            <a href="index.html" class="nk-nav-logo">
                <img src="{{ asset('Front/assets/images/logoE.png') }}" alt="GoodGames" width="199">
            </a>

            <ul class="nk-nav nk-nav-right d-none d-lg-table-cell" data-nav-mobile="#nk-nav-mobile">

                <li class=" nk-drop-item">
                    <a href="{{ path('global') }}">
                        Home
                    </a>
                </li>
                <li class="nk-drop-item">

                    <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('afficheequipe') }}"
                    {% else %}
                        "data-toggle="modal" data-target="#modalLogin"
                    {% endif %}
                    >Equipes</a>

                </li>

                {% if is_granted("ROLE_CLIENT") %}
                <li class=" nk-drop-item">
                    <a href="">

                    <i class="fa fa-user"></i>
                    </a>
                    <ul class="dropdown">

                        <li>
                            <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('app_logout') }}"
                            {% else %}
                                "data-toggle="modal" data-target="#modalLogin"
                            {% endif %}
                            >logout</a>
                        </li>
                        <li>
                            <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('m',{'id':app.user.id}) }}"
                            {% else %}
                                "data-toggle="modal" data-target="#modalLogin"
                            {% endif %}
                            >update</a>
                        </li>
                    </ul>
                </li>
{% endif %}
                <li class=" nk-drop-item">
                    <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('Affichepostfront') }}"
                    {% else %}
                        "data-toggle="modal" data-target="#modalLogin"
                    {% endif %}
                    >Blog</a>
                </li>
                <li class=" nk-drop-item">
                    <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('tournament') }}"
                    {% else %}
                        "data-toggle="modal" data-target="#modalLogin"
                    {% endif %}
                    >Compétitions</a><ul class="dropdown">

                        <li>
                            <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('tournament') }}"
                            {% else %}
                                "data-toggle="modal" data-target="#modalLogin"
                            {% endif %}
                            >Compétitions</a>
                        </li>
                        <li>
                            <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('games') }}"
                            {% else %}
                                "data-toggle="modal" data-target="#modalLogin"
                            {% endif %}
                            >Jeux</a>
                        </li>
                    </ul>
                </li>
                <li class=" nk-drop-item">
                    <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('affichfront') }}"
                    {% else %}
                        "data-toggle="modal" data-target="#modalLogin"
                    {% endif %}
                    >Store</a>
                </li>

                <li class=" nk-drop-item">
                    <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('Ajoutfront') }}"
                    {% else %}
                        "data-toggle="modal" data-target="#modalLogin"
                    {% endif %}
                    >contact us</a><ul class="dropdown">

                        <li>
                            <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('Ajoutfront') }}"
                            {% else %}
                                "data-toggle="modal" data-target="#modalLogin"
                            {% endif %}
                            >ajouter</a>
                        </li>
                        <li>
                            <a href="
                                    {% if is_granted("ROLE_CLIENT") %}
                                    {{ path('AfficheRfront') }}"
                            {% else %}
                                "data-toggle="modal" data-target="#modalLogin"
                            {% endif %}
                            >consulter</a>
                        </li>
                    </ul>
                </li>

            </ul>

            <ul class="nk-nav nk-nav-right nk-nav-icons">

                <li class="single-icon d-lg-none">
                    <a href="#" class="no-link-effect" data-nav-toggle="#nk-nav-mobile">
                                <span class="nk-icon-burger">
                                    <span class="nk-t-1"></span>
                                    <span class="nk-t-2"></span>
                                    <span class="nk-t-3"></span>
                                </span>
                    </a>
                </li>


            </ul>
        </div>
    </div>
</nav>


























