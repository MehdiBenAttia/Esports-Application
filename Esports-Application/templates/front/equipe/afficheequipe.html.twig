{% extends 'front.html.twig' %}
{% block body %}

    <body>

    <style>
        tr{
            background-color: #171E22;
        }
    </style>

    <div class="col-lg-12 stretch-card">
        <div class="card">
            <div class="card-body" style="background-color: #171E22">>
                <h4 class="card-title">Les Equipes E-Tsports</h4>
                <p class="card-description">
                    </code>
                </p>
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                        <tr>


                            <th>Nom Equipe</th>

                            <th>Email de L'equipe</th>
                            <th>Jeu de L'equipe</th>
                            <th>Users</th>
                            <th>delete</th>
                            <th>update</th>
                            <th>ajouter des joueurs </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for u in u %}
                            <tr >


                                <td>
                                    {{ u.nomEquipe }}
                                </td>

                                <td>
                                    {{ u.email}}
                                </td>

                                <td>
                                    {{ u.jeu}}
                                </td>
                                <td>

                                    {% for user in u.users %}

                                        {% if user is not null %}

                                            <br>
                                                {{ user.nom }}
                                            </br>
                                        {% endif %}
                                    {% endfor %}

                                </td>



                                <td>
                                    {% if is_granted("ROLE_CLIENT") and app.user.equipe==u %}
                                    <a href="{{ path('deletequipe',{'id':u.id}) }}">Delete</a> {% endif %}
                                </td>

                                <td>
                                    {% if is_granted("ROLE_CLIENT") and app.user.equipe==u %}
                                    <a href="

                                    {{ path('modifierequipe',{'id':u.id}) }}"


                                    >modifier</a>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if is_granted("ROLE_CLIENT") and app.user.equipe==u %}
                                    <a href="

                                    {{ path('noteam',{'id':u.id}) }}"


                                    >ajouter joueur à l'equipe</a>
                                    {% endif %}
                                </td>

                            </tr>
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    </div>
    </div>









    </table>

    <a href="{{ path('ajouterequipe') }}"class="btn-danger btn-lg" style="margin-left: 30%"> ajouter equipe</a>

    </body>
{% endblock %}





