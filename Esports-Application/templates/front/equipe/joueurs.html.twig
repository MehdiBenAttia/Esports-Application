{% extends 'front.html.twig' %}




{% block body %}
    <body>

    <style>
        tr{
            background-color: #171E22;
        }
    </style>

    <div class="col-lg-12 stretch-card">
        <div class="card">
            <div class="card-body" style="background-color: #171E22">>
                <h4 class="card-title">Les membres E-Tsports</h4>
                <p class="card-description">
                    </code>
                </p>
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>

                        </thead>
                        <tbody>
                        {% for user in u.users %}
                            <tr>




                                {% if user is not null %}
                                    <td>
                                        {{ user.nom }}

                                    </td>
                                {% endif %}




                                <td>
                                    <a href="{{ path('supprimerjoueurs',{'id':user.id}) }}"> supprimer</a>

                                </td>

                            </tr>

                        {% endfor %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    </div>
    </div>









    </table>
    <a href="{{ path('afficheequipe') }}"> Afficher Les Equipes</a>

    </body>
{% endblock %}














