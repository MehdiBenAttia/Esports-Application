{% extends 'base.html.twig' %}

{% block stylesheets %}

    <link href="{{ asset('vendors/feather/feather.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/ti-icons/css/themify-icons.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/css/vendor.bundle.base.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/datatables.net-bs4/dataTables.bootstrap4.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('vendors/ti-icons/css/themify-icons.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('js/select.dataTables.min.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('css/vertical-layout-light/style.css') }}" type="text/css" rel="stylesheet" />
    <link href="{{ asset('images/favicon.png') }}" type="text/css" rel="shortcut icon" />





{% endblock %}
{% block body %}
<body>



<div class="col-lg-12 stretch-card">
    <div class="card">
        <div class="card-body" ">
            <h4 class="card-title">Les membres E-Tsports</h4>


            <p class="card-description">
                </code>
            </p>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                    <tr>
                        <th>
                            ID
                        </th>
                        <th>
                            First name
                        </th>
                        <th>
                          Last name
                        </th>
                        <th>
                         Username
                        </th>
                        <th>
                          Email
                        </th>
                        <th>
                            Date De Naissance
                        </th>
                        <th>
                            Sexe
                        </th>
                        <th>
                            Equipe
                        </th>
                        <th>
                            Password
                        </th>
                        <th>
                            Delete
                        </th>
                        <th>
                           Modify
                        </th>
                        <th>
                            bloquer
                        </th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for u in u %}
                        <tr class="table-info">
                            <td>
                                {{ u.id }}
                            </td>

                            <td>
                                {{ u.nom }}
                            </td>

                            <td>
                                {{ u.prenom}}

                            </td>
                            <td>
                                {{ u.username}}
                            </td>
                            <td>
                                {{ u.email}}
                            </td>

                            <td>
                                {{ u.dateN |date('d-m-Y') }}
                            </td>

                            <td>
                                {{u.sexe}}
                            </td>

                            <td>
                                {% if u.equipe is not null %}

                                    {{u.equipe.NomEquipe}}

                                {% endif %}
                            </td>
                            <td>
                                {% if u.password is not null %}

                                    {{u.password}}

                                {% endif %}
                            </td>

                            <td>
                                <a href="{{ path('dback',{'id':u.id}) }}"> Delete</a>
                            </td>

                            <td>
                                <a href="{{ path('mback',{'id':u.id}) }}"> Modify</a>
                            </td>

                            <td>
                                {% if u.block==0 %}
                                <a href="{{ path('block',{'id':u.id}) }}"> Bloquer</a>
                                {% else %}
                                    <a href="{{ path('block',{'id':u.id}) }}"> Debloquer</a>
                                    {% endif %}
                            </td>
                        </tr>
                    {% endfor %}

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
</div>
</div>









</table>
    <a href="{{ path('aback') }}" > ajouter</a>
 <a href="{{ path('ajouterequipeback') }}"> ajouter equipe</a>

</body>
{% endblock %}
