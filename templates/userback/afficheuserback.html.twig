{% extends 'base.html.twig' %}


{% block body %}
<body>



<div class="col-lg-12 stretch-card">
    <div class="card">
        <div class="card-body" ">
            <h4 class="card-title">Les membres E-Tsports</h4>


            <p class="card-description">
                </code>
            </p>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                    <tr>
                        <th>
                            ID
                        </th>
                        <th>
                            First name
                        </th>
                        <th>
                          Last name
                        </th>
                        <th>
                          Email
                        </th>
                        <th>
                            Date De Naissance
                        </th>
                        <th>
                            Sexe
                        </th>
                        <th>
                            Equipe
                        </th>
                        <th>
                            Password
                        </th>
                        <th>
                            Delete
                        </th>
                        <th>
                           Modify
                        </th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for u in u %}
                        <tr class="table-info">
                            <td>
                                {{ u.id }}
                            </td>

                            <td>
                                {{ u.nom }}
                            </td>

                            <td>
                                {{ u.prenom}}
                            </td>

                            <td>
                                {{ u.email}}
                            </td>

                            <td>
                                {{ u.dateN |date('d-m-Y') }}
                            </td>

                            <td>
                                {{u.sexe}}
                            </td>

                            <td>
                                {% if u.equipe is not null %}

                                    {{u.equipe.NomEquipe}}

                                {% endif %}
                            </td>
                            <td>
                                {% if u.password is not null %}

                                    {{u.password}}

                                {% endif %}
                            </td>

                            <td>
                                <a href="{{ path('dback',{'id':u.id}) }}"> Delete</a>
                            </td>

                            <td>
                                <a href="{{ path('mback',{'id':u.id}) }}"> Modify</a>
                            </td>

                        </tr>
                    {% endfor %}

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
</div>
</div>









</table>
    <a href="{{ path('aback') }}" > ajouter</a>
 <a href="{{ path('ajouterequipe') }}"> ajouter equipe</a>

</body>
{% endblock %}
