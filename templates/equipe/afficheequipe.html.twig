{% extends 'basefront.html.twig' %}




{% block body %}






    <body>

    <style>
        tr{
            background-color: #171E22;
        }
    </style>

    <div class="col-lg-12 stretch-card">
        <div class="card">
            <div class="card-body" style="background-color: #171E22">>
                <h4 class="card-title">Les Equipes E-Tsports</h4>
                <p class="card-description">
                    </code>
                </p>
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                        <tr>

                            <th>ID</th>
                            <th>Nom Equipe</th>

                            <th>Email de L'equipe</th>
                            <th>Jeu de L'equipe</th>
                            <th>Users</th>
                            <th>delete</th>
                            <th>update</th>
                            <th>ajouter des joueurs </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for u in u %}
                            <tr >
                                <td>
                                    {{ u.id }}
                                </td>

                                <td>
                                    {{ u.nomEquipe }}
                                </td>

                                <td>
                                    {{ u.email}}
                                </td>

                                <td>
                                    {{ u.jeu}}
                                </td>
                                <td>

                                    {% for user in u.users %}

                                        {% if user is not null %}

                                            <br>
                                                {{ user.nom }}
                                            </br>
                                        {% endif %}
                                    {% endfor %}

                                </td>



                                <td>
                                    <a href="{{ path('deletequipe',{'id':u.id}) }}"> Delete</a>
                                </td>

                                <td>
                                    <a href="{{ path('modifierequipe',{'id':u.id}) }}"> Modify</a>
                                </td>
                                <td>
                                    <a href="{{ path('noteam',{'id':u.id}) }}"> ajouter joueur a l'equipe</a>
                                </td>

                            </tr>
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    </div>
    </div>









    </table>
    <a href="{{ path('a') }}"> ajouter</a>
    <a href="{{ path('ajouterequipe') }}"> ajouter equipe</a>

    </body>
{% endblock %}





